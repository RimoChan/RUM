# RUM的训练文档

首先要有Python和torch。我的版本是Python3.10和torch 2.9.1+cu130，这个代码应该不挑版本，自己的能跑就行。

首先`pip install -r requirements.txt`装1下依赖，然后`train.sh`里面是启动命令，里面的参数基本上就是字面上的意思，大家有训过原版的应该都比较清楚。


## 训练的参数解释

首先是几个路径`pretrained_model_name_or_path`，`teacher_model_name_or_path`，`train_data_dir`，`output_dir`填自己本地的路径就可以了。

`learning_rate`测试下来比较好的范围大概是在1e-6到5e-7，这个指的是Adam的学习率，使用Muon的层会自动乘上40倍，或者也可以单独用`learning_rate_muon`参数配置。

`optimizer`也建议选muon。

`mixed_precision`选bf16，因为只支持bf16。

然后是几个概率，我的配置是`drop_tag_rate=0.1`，`drop_text_rate=0.05`，`drop_char_feature_rate=0.6`、`学人rate=0.3`、`downsample_rate=0.3`、`edit_rate=0.15`。
- `drop_tag_rate`指的是在prompt预处理中，单个tag被丢掉的概率。
- `drop_text_rate`就是训练negative的概率。
- `drop_char_feature_rate`指的是在prompt预处理中，当角色的名字出现的时候，丢弃与她强关联的标签的概率，这是另外1个很神奇的东西，实现原理可以看[这里](https://github.com/RimoChan/waifu-sensor)。
- `学人rate`指的是在prompt预处理中，丢弃大部分标签只留下人物名字的概率。
- `downsample_rate`指的是将latent和noise同时向下采样2倍或者4倍的概率。
- `edit_rate`指的是从编辑数据集里拿训练样本的概率。如果你没有编辑数据集，那就只能设成0了。

然后是几个教师模型推理的相关配置，`inference_steps`、`teacher_cfg`、`max_size`、`min_size`，就按平时推理的参数来写就可以了。不过应该大家都差不多吧，比如inference_steps设25，cfg设7这样。

然后是`prefetch_cache_dir`。如果嫌在线处理教师模型慢的话，`data.py`的main里有1个刷数据的脚本，可以先用那个把数据刷到硬盘上，然后`prefetch_cache_dir`就填你刷出来那个路径，当然，教师模型推理的相关配置就变成固定是刷的时候用的那些，再填就失效了。

其他的参数应该和训练标准Diffusion没什么区别，大家按照自己的喜好填写。


## 1些小提示

- 训练数据，就是前文里提到的100k数据，是用`小工具/dan2024均衡.py`抽取的。

- 如果不训编辑的话，编辑能力会很快消失，变成只能出灰色的图。
    - 我是在1180k step的checkpoint按64%的比例和原版混合，然后掺杂了15%的MagicBrush的编辑数据继续训的。

- 原版的Muon在纯BF16上其实是不更新的，当时是在800k step的时候发现它的精度不够，实际上之前是把学习率开得非常大，它才勉强能更新，所以效果不好。
    - `requirements.txt`里的安装的Muon其实不是原始仓库，而是我自己魔改的版本。

- 画师串其实并没有按照prompt来学，而是做了1个重组的操作。在构造prompt的时候会直接把画师丢掉，然后从预定义的列表里随机抽1~2个画师组装上去。

- 模型的定义之所以叫「哭」，是因为汉字是象形文字。
